{% extends 'base.html.twig' %}

{% block title %}Category quizzes{% endblock %}

{% block body %}
    <h1>{{ category.name }}</h1>
    <div class="quizzes mt-5">
        <div class="row">
            <div class="col-lg-8 m-auto">
                <div class="cards d-flex">
                    {% for quiz in quizzes %}
                        <div class="card" style="width: 18rem; min-width: 20%;">
                            <img src="https://www.php.net/images/meta-image.png" class="card-img-top" alt="...">
                            <div class="card-body">
                                <h5 class="card-title">{{ quiz.title }}</h5>
                                <p class="card-text">{{ quiz.summary ?? 'desc' }}</p>
                                <a href="#" class="btn btn-primary">Пройти</a>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
